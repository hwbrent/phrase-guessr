/**
 * A `Document` for the page at {@link https://www.omniglot.com/soundfiles/}
 */
export type HomeDocument = Document;

/**
 * A `Document` for the page with url of the form
 * `https://www.omniglot.com/language/phrases/<language>.php`
 */
export type PagesDocument = Document;
